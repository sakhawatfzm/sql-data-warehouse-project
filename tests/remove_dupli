SELECT cst_id, COUNT(*) 
FROM silver.crm_cust_info
GROUP BY cst_id
HAVING COUNT(*) > 1;
-- Result should be empty

Check the standardized values:

SELECT DISTINCT cst_material_status 
FROM silver.crm_cust_info;
-- Result should only be: 'Single', 'Married', 'n/a'
